import { StyleSheet, Text, View } from "react-native";
import React from "react";

export default function ImageBackgroundHeader() {
  return (
    <View>
      <Text>ImageBackgroundHeader</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
